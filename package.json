{
  "name": "cloudphotos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd client && npm run build",
    "deploy": "vercel --prod",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "form-data": "^4.0.0",
    "busboy": "^1.6.0"
  }
}
